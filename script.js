/**
 * Kyro Zhao · Personal Site
 * Minimal micro-interactions (optional enhancement)
 */

document.addEventListener('DOMContentLoaded', () => {
  // Subtle fade-in on load (optional)
  document.body.classList.add('loaded');
});
